{
  "name": "q",
  "version": "1.0.0",
  "main": "index.js",
  "author": "kenrick",
  "license": "MIT",
  "scripts": {
    "watch-tsc": "make tsc && fswatch -d src/ts make | xargs -n1 -I{} make tsc",
    "watch-cpp": "make cpp && fswatch -d src/cpp make | xargs -n1 -I{} make cpp"
  },
  "devDependencies": {
    "@types/emscripten": "^1.39.4",
    "esbuild": "^0.5.20"
  }
}
